package ru.ivan.ver.common;

/**
 * Класс, содержащий константы с названиями топиков.
 * Используется для централизованного хранения и управления именами топиков в приложении.
 * Все поля объявлены как {@code public static final}, так как представляют собой константы,
 * которые должны быть доступны из других классов без создания экземпляра этого класса.
 */
public class TopicList {
    /**
     * Основной топик.
     */
    public static String MAIN_TOPIC = "yandex.practicum.task-2.topic";
}
